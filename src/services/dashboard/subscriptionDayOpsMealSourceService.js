"use strict";

// Backward-compatible dashboard import. The resolver is shared with the
// Flutter pickup flow so both clients read the same customer meal selections.
module.exports = require("../subscription/subscriptionDayMealSourceService");
