// Legacy CI entry point kept temporarily so existing workflows continue to run.
// The active contract is the single Flutter/Dashboard-compatible v3 contract.
require("./mealPlannerDashboardCompatibility.test");
