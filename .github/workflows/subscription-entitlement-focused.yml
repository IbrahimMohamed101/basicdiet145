name: Subscription Entitlement Focused Test

# Verification-only branch change; workflow logic is identical to main.
on:
  push:
    branches: [main]
    paths:
      - "src/models/Subscription.js"
      - "src/models/SubscriptionDay.js"
      - "src/models/SubscriptionPickupRequest.js"
      - "src/services/subscription/**"
      - "src/services/fulfillmentService.js"
      - "src/services/dashboard/opsTransitionService.js"
      - "tests/subscriptionMealEntitlementLifecycle.test.js"
      - ".github/workflows/subscription-entitlement-focused.yml"
  pull_request:
    paths:
      - "src/models/Subscription.js"
      - "src/models/SubscriptionDay.js"
      - "src/models/SubscriptionPickupRequest.js"
      - "src/services/subscription/**"
      - "src/services/fulfillmentService.js"
      - "src/services/dashboard/opsTransitionService.js"
      - "tests/subscriptionMealEntitlementLifecycle.test.js"
      - ".github/workflows/subscription-entitlement-focused.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  focused-entitlement-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run focused lifecycle test
        env:
          NODE_ENV: test
        run: node tests/subscriptionMealEntitlementLifecycle.test.js
