
services:
  api:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # Atlas-only: must be provided via .env or host environment
      MONGO_URI: ${MONGO_URI}
      MONGO_DB: basicdiet145
    restart: always
